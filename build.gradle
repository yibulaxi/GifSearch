// Top-level build file where you can add configuration options common to all sub-projects/modules.

ext{
    applicationId = "com.allever.app.template"
    minSdkVersion = 19
    compileSdkVersion = 28
    targetSdkVersion = 28
    versionCode = 1
    versionName = "1.0"

    umengAppKey = "5da6c299570df31978000f59"
    admobAppId = "ca-app-pub-5576030072710638~7900912228"

    //C:\Users\Allever\AndroidStudioProjects/com.allever.app.jks
    signingStoreFile = '../../com.allever.app.jks'
    signingKeyAlias = 'allever'
    signingStorePassword = 'dixm5400'
    signingKeyPassword = 'dixm5400'
}

buildscript {
    ext.kotlin_version = '1.3.20'
    repositories {
        google()
        jcenter()
        
    }
    dependencies {
        classpath 'com.android.tools.build:gradle:3.4.1'
        classpath "org.jetbrains.kotlin:kotlin-gradle-plugin:$kotlin_version"
        // NOTE: Do not place your application dependencies here; they belong
        // in the individual module build.gradle files
    }
}

allprojects {
    repositories {
        google()
        jcenter()
        
    }
}

task clean(type: Delete) {
    delete rootProject.buildDir
}
